name: fat_call
description: Try to make call with FCM.

publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^1.0.1
  firebase_core: ^1.0.1
  google_sign_in: ^5.0.1
  cloud_firestore: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
#  flutter_launcher_icons:
#    git: https://github.com/personalizedrefrigerator/flutter_launcher_icons
#  flutter_native_splash:
#    git: https://github.com/jonbhanson/flutter_native_splash
  pedantic: ^1.11.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png